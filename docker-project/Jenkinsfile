pipeline {
    agent any
    
    stages {
        stage('Checkout Code') {
            steps {
                // Faz o checkout do código da branch main
                git branch: 'main', url: 'https://github.com/OsmOVB/Devops_Automation_Flask_mariadb.git'
            }
        }
        
        stage('Build Docker Images') {
            steps {
                dir('docker-project') {
                    // Comando para construir as imagens dos containers
                    sh 'docker-compose build'
                }
            }
        }
        
        stage('Run Containers') {
            steps {
                dir('docker-project') {
                    // Comando para iniciar os containers em segundo plano
                    sh 'docker-compose up -d'
                }
            }
        }
        
        stage('Application Logs') {
            steps {
                dir('docker-project') {
                    // Exibe os logs do container do Flask e MariaDB
                    sh 'docker logs flask_app_container || echo "Flask container log retrieval failed"'
                    sh 'docker logs mariadb_container || echo "MariaDB container log retrieval failed"'
                }
            }
        }

        
        stage('Check Application Status') {
            steps {
                script {
                    // Verifica se a aplicação está acessível em localhost:5000
                    echo "Aplicação Flask disponível em http://localhost:5000"
                }
            }
        }
    }
}
